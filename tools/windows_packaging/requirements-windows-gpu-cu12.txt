# NVIDIA GPU / CUDA 12-oriented Windows build/runtime dependency set.
# v69.6 pins the GPU runtime stack. Do not leave these unpinned: newer
# onnxruntime-gpu wheels may target CUDA 13 DLL names (cublasLt64_13.dll,
# cudart64_13.dll) and break the portable CUDA 12 GPU build.
#
# Note: build_windows_gui.ps1 installs GPU requirements in a controlled order.
# It intentionally does not install requirements-windows-common.txt directly,
# because upstream insightface would otherwise pull transient CPU onnxruntime.
onnxruntime-gpu==1.26.0
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cudnn-cu12==9.23.1.3
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
nvidia-nvjitlink-cu12==12.9.86
